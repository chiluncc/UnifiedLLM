[project]
name = "unified-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chiluncc", email = "1431530893@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openai>=3.6.0",
]

[project.scripts]
unified-llm = "unified_llm:main"

[build-system]
requires = ["uv_build>=0.11.29,<0.12.0"]
build-backend = "uv_build"
